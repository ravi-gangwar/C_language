#include <stdio.h>
int main(){
    int num, a=0, b=1;
    printf("Enter a number to value.\n:");
    scanf("%d", num);
    if (num==1||num==2)
    {
        printf(1);

    }
    else
        for (int i=1; i<=num; i++){
            c=a+b;   
        }
}